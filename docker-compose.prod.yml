version: '3.8'
services:
  mongodb:
    container_name: lunchbot-database
    image: mongo
    volumes:
      - lunchbot_production:/data/db
    ports:
      - '27017:27017'
  backend:
    container_name: lunchbot-backend
    build:
      context: ./
      dockerfile: ./docker/Dockerfile.prod
    depends_on:
      - mongodb
    ports:
      - '80:80'
    env_file:
      - ./env/common.prod.env
      - ./env/secrets.env
volumes:
  lunchbot_production:
    external: false
