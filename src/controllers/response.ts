import { ActivityType } from '../types/activityType'

export const handleJoinDeclineResponse = (activityType: ActivityType, userId: string, status: string): void => {
  // Todo: Create args into object for readability.
  // 1. Register response in DB
  // 2. Send out message to user
}
